module.exports = {
    plugins: {
        // include whatever plugins you want
        // but make sure you install these via yarn or npm!
		
		// also add browserslist config to package.json for plugins that need it
		// (see browserslist github for details)
        
        autoprefixer: {}
    }
};
